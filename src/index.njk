---
layout: "layouts/base.njk"
---

<h2>Projects</h2>

<ul>
    {% for project, projects in collections.projects %}
    <li>
        <a href="projects/{{ project | slugify }}">{{ project }} ({{ projects.length }} issues)</a>
    </li>
    {% endfor %}
</ul>
